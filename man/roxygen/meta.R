list(
  markdown = TRUE,
  knitr_chunk_options = list(
    cache = TRUE,
    cache_lazy = FALSE,
    cache.path = "man/_cache/",
    error = TRUE,
    fig.process = base::basename
  ),
  restrict_image_formats = TRUE
)
