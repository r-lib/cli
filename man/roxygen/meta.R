list(
  markdown = TRUE,
  knitr_chunk_options = list(
    cache = TRUE,
    error = TRUE,
    cache.path = "man/_cache/",
    cache.lazy = FALSE,
    fig.path = "man/figures/",
    fig.process = base::basename
  )
)
